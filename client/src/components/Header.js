import React from 'react';
import Search from './Search';

const Header = React.createClass ({
  render () {
    return (
      <div>
        <Search />
      </div>
    );
  }
});

export default Header;